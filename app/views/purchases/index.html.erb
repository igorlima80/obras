<%= render 'shared/card_list',
  list: @purchases,
  order: 'id',
  sort_all: true,
  show_new: true,
  card_class: 'info',
  table_class: 'bootstrap-table-mobile',
  show_columns: [
    'id',
    'expected_date',
    'purchase_date',
    'construction.name',
    'sub_stage.name',
    'provider.name',
    'status'
  ]
%>
